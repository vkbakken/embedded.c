#include <stdint.h>
#include <stdbool.h>

#include <led.h>


void board_led_off(uint8_t led_no)
{
    switch (led_no) {
        case 0:
        break;
    }
}


void board_led_on(uint8_t led_no)
{
    switch (led_no) {
        case 0:
        break;
    }
}
